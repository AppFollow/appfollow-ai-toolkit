---
description: Find an app on the App Store / Google Play via the AppFollow MCP and start tracking it, then hand structured app refs to the report skills.
argument-hint: "[app name | store URL | ext_id]"
---

Use the `search` skill (`skills/search/SKILL.md`) to find and track apps for the user. Input: "$ARGUMENTS".
